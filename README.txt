FILES:
1.classifier.py - A file that responsible for loading the data and and prosseses it. Also, the two main functions are
in this file - 'predict' and 'send_police_cars'.
2. fit.py - A file in which we execute the random forest algorithm.
3. project.pdf - A wrriten description of our project.
4. weights.p - The weights of the fitted random forest model.
5. columns.p - In order to save the format of the coloumns of the trained data.
6. data.p - We need to save the data in order to predict the area of the crimes.
7. requirements.txt - The requirements for the virtual env.
8. USERS.txt- The users names and id.